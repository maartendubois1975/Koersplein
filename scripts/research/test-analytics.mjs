import {buildBaseRates,buildPriceFeatures,makeChronologicalSplits,scoreForward} from './analytics.mjs';
const bars=[];for(let i=0;i<900;i++){const d=new Date(Date.UTC(2020,0,1+i));bars.push({date:d.toISOString().slice(0,10),close:100+i*.1});}const s={bars};const r=buildBaseRates(s,{horizons:[3],targets:[10]});if(r.length!==1||!r[0].sample_size)throw new Error('base rates');const f=buildPriceFeatures(s,500);if(!Number.isFinite(f.return_252d_pct))throw new Error('features');const sp=makeChronologicalSplits(bars);if(!(sp.train_end<sp.validation_end&&sp.validation_end<sp.test_start))throw new Error('split');const o=scoreForward(s,100,3,5);if(!o)throw new Error('outcome');console.log('Research analytics core: PASS');
