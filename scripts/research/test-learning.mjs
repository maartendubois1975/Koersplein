import {hindsightDiagnostics,deriveCandidateRule,evaluateRule} from './learning.mjs';const rows=[];for(let i=0;i<120;i++)rows.push({features:{return_21d_pct:i},outcome:{target_hit:i>70,end_return_pct:i-60}});const d=hindsightDiagnostics(rows),r=deriveCandidateRule(d,{minSamples:20,minSpread:.05}),e=evaluateRule(rows,r);if(!r||!(e.lift>0))throw new Error('learning gate');console.log('Hindsight/learning core: PASS');
