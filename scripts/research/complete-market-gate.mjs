import fs from 'node:fs/promises';const mic=process.env.MARKET_MIC;if(!mic)throw new Error('MARKET_MIC ontbreekt');const plan=JSON.parse(await fs.readFile('data/world-fill-plan.json','utf8'));const m=plan.markets.find(x=>x.mic===mic);if(!m)throw new Error('Onbekende markt '+mic);if(m.state!=='COMPLETE')throw new Error(`${mic} is ${m.state}; analyse start pas na COMPLETE eindvalidatie`);const raw=JSON.parse(await fs.readFile(`data/euronext-${m.code}.json`,'utf8'));const shares=raw.shares||raw.instruments||[];if(!shares.length)throw new Error('Catalogus leeg');const isins=new Set(shares.map(x=>x.isin));if(isins.size!==shares.length)throw new Error('Dubbele ISIN in catalogus');console.log(JSON.stringify({ok:true,mic,market:m.name,instruments:shares.length,state:m.state}));