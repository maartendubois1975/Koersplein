import crypto from 'node:crypto';
export function modelManifest(x){const m={modelId:x.modelId,version:x.version,status:x.status||'CHALLENGER',featureSet:[...(x.featureSet||[])].sort(),trainingWindow:x.trainingWindow,foldContract:x.foldContract,hyperparameters:x.hyperparameters||{},calibration:x.calibration||{},evidence:x.evidence||{},dataSnapshot:x.dataSnapshot,codeCommit:x.codeCommit,createdAt:x.createdAt};m.manifestHash=crypto.createHash('sha256').update(JSON.stringify(m)).digest('hex');return m;}
export function mayPromote(m){const e=m.evidence||{};return Boolean(e.machine3State==='OOS_SURVIVOR'&&e.independentHoldoutGain===true&&e.noRiskRegression===true&&m.dataSnapshot&&m.codeCommit);}
export function promote(champion,challenger){if(!mayPromote(challenger))throw new Error('Challenger voldoet niet aan onafhankelijke promotiepoort');return{previousChampion:champion?{modelId:champion.modelId,version:champion.version}:null,champion:{...challenger,status:'CHAMPION'},promotedAt:new Date().toISOString()};}
