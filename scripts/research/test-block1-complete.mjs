import {validatePointInTimeEvents,eventsAvailableAt,eventFeatures} from './external-signals.mjs';import {calibrationGate} from './calibration.mjs';import {finalPromotionGate} from './promotion-gate.mjs';const e=validatePointInTimeEvents([{id:'a',available_at:'2020-01-02',source:'fixture',family:'rates',market:'PARIS',signed_score:1},{id:'b',available_at:'2020-02-02',source:'fixture',family:'macro',market:'PARIS'}]);if(eventsAvailableAt(e,'2020-01-15').length!==1)throw new Error('future event leaked');if(eventFeatures(e).external_event_count!==2)throw new Error('event features');const rows=Array.from({length:120},(_,i)=>({probability:i%2?.8:.2,outcome:{target_hit:!!(i%2)}}));if(!calibrationGate(rows,{maxBrier:.1,maxMeanGap:.01}).pass)throw new Error('calibration');const g=finalPromotionGate({leakage:0,provenance:true,holdout:true,calibration:true,externalCoverage:true,conclusionPresent:true,reproducible:true});if(!g.promoted)throw new Error('promotion');console.log('Research block 1 complete guards: PASS');
