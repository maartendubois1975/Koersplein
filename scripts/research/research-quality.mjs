export function outcome(bars,i,target,horizonMonths){const base=bars[i]?.close;if(!base)return null;const end=new Date(bars[i].date);end.setUTCMonth(end.getUTCMonth()+horizonMonths);let hit=false,timeToHitDays=null,maxUpside=-Infinity,maxDrawdown=0,seen=0;for(let j=i+1;j<bars.length&&new Date(bars[j].date)<=end;j++){seen++;const r=bars[j].close/base-1;maxUpside=Math.max(maxUpside,r);maxDrawdown=Math.min(maxDrawdown,r);if(!hit&&r>=target){hit=true;timeToHitDays=Math.round((new Date(bars[j].date)-new Date(bars[i].date))/86400000)}}return{hit:hit?1:0,timeToHitDays,maxUpside:Number.isFinite(maxUpside)?maxUpside:null,maxDrawdown,censored:seen===0||new Date(bars.at(-1).date)<end}}
export function wilson(k,n,z=1.959963984540054){if(!n)return[null,null];const p=k/n,d=1+z*z/n,c=(p+z*z/(2*n))/d,m=z*Math.sqrt((p*(1-p)+z*z/(4*n))/n)/d;return[Math.max(0,c-m),Math.min(1,c+m)]}
export function chronologicalFolds(rows,minTrain=.5,folds=4){const a=[...rows].sort((x,y)=>x.date.localeCompare(y.date)),out=[];for(let f=0;f<folds;f++){const cut=Math.floor(a.length*(minTrain+(1-minTrain)*f/folds)),end=Math.floor(a.length*(minTrain+(1-minTrain)*(f+1)/folds));if(end>cut)out.push({train:a.slice(0,cut),test:a.slice(cut,end)})}return out}
export function bh(pvalues,alpha=.05){const a=pvalues.map((p,i)=>({p,i})).filter(x=>Number.isFinite(x.p)).sort((x,y)=>x.p-y.p);let max=-1;for(let k=0;k<a.length;k++)if(a[k].p<=alpha*(k+1)/a.length)max=k;const keep=new Set(max<0?[]:a.slice(0,max+1).map(x=>x.i));return pvalues.map((_,i)=>keep.has(i))}
export function promotion({selected,experimentLift,baselineLift,ciLow=0,fdrPass=true}){if(selected<30)return{promote:false,reason:'TOO_FEW_SELECTED'};if(!fdrPass)return{promote:false,reason:'MULTIPLE_TESTING_GUARD'};if(!(experimentLift>baselineLift))return{promote:false,reason:'DID_NOT_BEAT_MOMENTUM'};if(!(ciLow>0))return{promote:false,reason:'LIFT_NOT_CONFIDENTLY_POSITIVE'};return{promote:true,reason:'READY_FOR_NEXT_GATE'}}