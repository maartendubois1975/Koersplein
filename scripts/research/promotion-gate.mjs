export function finalPromotionGate({leakage,provenance,holdout,calibration,externalCoverage,conclusionPresent,reproducible}){const checks={no_leakage:leakage===0,provenance_complete:provenance===true,unseen_holdout:holdout===true,calibration:calibration===true,external_point_in_time_coverage:externalCoverage===true,human_conclusion:conclusionPresent===true,reproducible:reproducible===true};const failed=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);return {status:failed.length?'FAIL':'PASS',promoted:failed.length===0,checks,failed};}
