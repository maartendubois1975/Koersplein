import fs from 'node:fs/promises';
const root=process.argv[2]||'research/output/machine2-deep';const dir=`${root}/dossiers`;const files=(await fs.readdir(dir)).filter(f=>f.endsWith('.json'));const stats=new Map();
const add=(key,correct,ret)=>{const s=stats.get(key)||{n:0,correct:0,sumReturn:0};s.n++;s.correct+=correct?1:0;s.sumReturn+=Number.isFinite(ret)?ret:0;stats.set(key,s)};
for(const f of files){const d=JSON.parse(await fs.readFile(`${dir}/${f}`,'utf8'));for(const p of d.predictions||[]){for(const [h,pred] of Object.entries(p.predictions||{})){const actual=p.realized?.[h];if(!Number.isFinite(pred)||!Number.isFinite(actual))continue;add(`BASELINE|${h}`,Math.sign(pred)===Math.sign(actual),actual);for(const e of d.evidence||[]){if(!e.pointInTimeEligible)continue;const val=e.normalizedSignal??e.value;if(!Number.isFinite(val))continue;const bucket=val>0?'POS':val<0?'NEG':'ZERO';add(`${e.family}|${bucket}|${h}`,Math.sign(pred)===Math.sign(actual),actual);}}}}
const candidates=[...stats.entries()].map(([key,s])=>({key,n:s.n,directionAccuracy:s.correct/s.n,meanRealizedReturn:s.sumReturn/s.n})).sort((a,b)=>b.n-a.n);const hypotheses=candidates.filter(x=>!x.key.startsWith('BASELINE')&&x.n>=30).map(x=>({...x,status:'CANDIDATE_ONLY',promotionForbidden:true,nextStep:'BLIND_OUT_OF_SAMPLE_TEST'}));await fs.writeFile(`${root}/learning-summary.json`,JSON.stringify({generatedAt:new Date().toISOString(),dossiers:files.length,candidates,hypotheses,rule:'Geen kandidaat wordt modelgewicht zonder nieuwe blinde out-of-sample test.'},null,2));console.log(`dossiers=${files.length} hypotheses=${hypotheses.length}`);