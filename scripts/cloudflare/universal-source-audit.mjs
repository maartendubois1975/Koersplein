import fs from 'node:fs/promises';
import {YahooChartProvider} from '../history/providers/yahoo-chart.mjs';
import {EuronextLiveProvider} from '../history/providers/euronext-live.mjs';
import {YahooIsinProvider} from '../history/providers/yahoo-isin.mjs';
import {StockAnalysisBitProvider} from '../history/providers/stockanalysis-bit.mjs';

const mic=process.env.MARKET_MIC;if(!mic)throw new Error('MARKET_MIC vereist');
const plan=JSON.parse(await fs.readFile('data/world-fill-plan.json','utf8')),m=plan.markets.find(x=>x.mic===mic);if(!m)throw new Error('Onbekende markt '+mic);
const cat=JSON.parse(await fs.readFile(`data/euronext-${m.code}.json`,'utf8')),shares=cat.shares||[];
const yahoo=new YahooChartProvider(),euronext=new EuronextLiveProvider(),yahooIsin=new YahooIsinProvider(),stockanalysis=new StockAnalysisBitProvider(),end=new Date().toISOString().slice(0,10),start=new Date(Date.now()-5*365.25*86400000).toISOString().slice(0,10);
const test=async x=>{const i={...x,ticker:x.symbol},out={isin:x.isin,symbol:x.symbol,mic:x.mic,name:x.name,yahoo:{ok:false},euronext:{ok:false},yahooIsin:{ok:false},stockanalysis:{ok:false}};try{const r=await yahoo.fetchDaily(i,{startDate:start,endDate:end});out.yahoo={ok:r.bars.length>0,bars:r.bars.length,first:r.bars[0]?.date,last:r.bars.at(-1)?.date}}catch(e){out.yahoo.error=e.message}try{const r=await euronext.fetchDaily(i,{startDate:start,endDate:end});out.euronext={ok:r.bars.length>0,bars:r.bars.length,first:r.bars[0]?.date,last:r.bars.at(-1)?.date}}catch(e){out.euronext.error=e.message}if(!out.yahoo.ok&&!out.euronext.ok){try{const r=await yahooIsin.fetchDaily(i,{startDate:start,endDate:end});out.yahooIsin={ok:r.bars.length>0,bars:r.bars.length,first:r.bars[0]?.date,last:r.bars.at(-1)?.date}}catch(e){out.yahooIsin.error=e.message}if(!out.yahooIsin.ok&&stockanalysis.supports(i)){try{const r=await stockanalysis.fetchDaily(i,{startDate:start,endDate:end});out.stockanalysis={ok:r.bars.length>0,bars:r.bars.length,first:r.bars[0]?.date,last:r.bars.at(-1)?.date}}catch(e){out.stockanalysis.error=e.message}}}return out};
const results=[];for(let n=0;n<shares.length;n+=4)results.push(...await Promise.all(shares.slice(n,n+4).map(test)));
const combined=results.filter(x=>x.yahoo.ok||x.euronext.ok||x.yahooIsin.ok||x.stockanalysis.ok),yahooOk=results.filter(x=>x.yahoo.ok),euronextOk=results.filter(x=>x.euronext.ok),fallbackOk=results.filter(x=>!x.yahoo.ok&&x.euronext.ok),uncovered=results.filter(x=>!x.yahoo.ok&&!x.euronext.ok&&!x.yahooIsin.ok&&!x.stockanalysis.ok),crossChecked=results.filter(x=>x.yahoo.ok&&x.euronext.ok),difficult=[...results].sort((a,b)=>(a.yahoo.ok?1:0)-(b.yahoo.ok?1:0)).slice(0,Math.min(20,results.length)),ratio=shares.length?combined.length/shares.length:0,crossRatio=shares.length?crossChecked.length/shares.length:0;
// The canonical end gate permits zero missing/invalid instruments. Source approval must
// therefore prove a usable route for every catalog instrument; a 95% preflight only
// postpones a known failure until after an expensive backfill.
const explicitDataUnavailableByMic={
  XETR:new Map([['DE000A41YEE3',{symbol:'BRN',name:'BRANICKS GROUP AG NA JGE',reason:'NO_HISTORY_AFTER_FOUR_INDEPENDENT_ROUTE_TESTS'}]])
};
const allowedMap=explicitDataUnavailableByMic[mic]||new Map();
const dataUnavailable=uncovered.filter(x=>allowedMap.has(x.isin)).map(x=>({...x,status:'DATA_UNAVAILABLE',reason:allowedMap.get(x.isin).reason}));
const unresolved=uncovered.filter(x=>!allowedMap.has(x.isin));
const pass=shares.length>=5&&difficult.length>=10&&unresolved.length===0&&combined.length+dataUnavailable.length===shares.length;
const report={generatedAt:new Date().toISOString(),market:mic,catalogSource:cat.source||null,catalogFingerprint:cat.fingerprint||null,catalog:shares.length,range:{start,end},sourcePolicy:{minimumCombinedCoverage:1,maximumUncovered:0,minimumDifficultSymbols:10,independentProvidersTested:['yahoo-chart','euronext-live','yahoo-isin-resolver','stockanalysis-bit'],discoveryPolicy:'Require every official-catalog instrument to be either source-covered or explicitly proven DATA_UNAVAILABLE; unresolved instruments still block backfill.',requireOfficialCatalog:true},yahooCovered:yahooOk.length,euronextCovered:euronextOk.length,euronextFallbackCovered:fallbackOk.length,crossChecked:crossChecked.length,crossCheckRatio:crossRatio,combinedCovered:combined.length,coverageRatio:ratio,uncovered,dataUnavailable,unresolved,difficultTested:difficult.length,difficult,pass};
await fs.mkdir(`research/output/${m.code}`,{recursive:true});await fs.writeFile(`research/output/${m.code}/source-audit.json`,JSON.stringify(report,null,2));
const p='data/market-source-registry.json',reg=JSON.parse(await fs.readFile(p,'utf8'));reg.markets||={};
if(pass){reg.markets[mic]={status:'APPROVED',catalogFingerprint:cat.fingerprint||null,catalogSource:cat.source||`Official ${m.name} equity directory`,historySources:[{provider:'yahoo-isin-resolver',role:'DISCOVERY_FALLBACK',note:'ISIN-first resolver used when direct symbol and official history route fail.'},{provider:'yahoo-chart',role:'PRIMARY',note:`Full-catalog preflight: ${yahooOk.length}/${shares.length} instruments.`},{provider:'euronext-live',role:'INDEPENDENT_FALLBACK_VALIDATION',note:`Independently tested on full catalog: ${euronextOk.length}/${shares.length}; ${fallbackOk.length} additional instruments beyond primary.`}],discovery:{testedDifficultSymbols:difficult.length,testedFullCatalog:shares.length,coverageRatio:ratio,crossCheckedSymbols:crossChecked.length,crossCheckRatio:crossRatio,sourceCountTested:4,note:'Official catalog is tested before backfill. Every instrument must be source-covered or explicitly proven DATA_UNAVAILABLE; unresolved instruments block approval.'}};}else{reg.markets[mic]={...(reg.markets[mic]||{}),status:'BLOCKED',catalogFingerprint:cat.fingerprint||null,catalogSource:cat.source||reg.markets[mic]?.catalogSource,blockedReason:'SOURCE_PREFLIGHT_GATE',lastAuditAt:new Date().toISOString(),discovery:{...(reg.markets[mic]?.discovery||{}),testedDifficultSymbols:difficult.length,testedFullCatalog:shares.length,coverageRatio:ratio,crossCheckedSymbols:crossChecked.length,crossCheckRatio:crossRatio,sourceCountTested:4,uncovered:uncovered.length,dataUnavailable:dataUnavailable.length,unresolved:unresolved.length}};}
await fs.writeFile(p,JSON.stringify(reg,null,2)+'\n');
if(!pass)process.exitCode=2;console.log(JSON.stringify({market:mic,catalog:shares.length,yahoo:yahooOk.length,euronext:euronextOk.length,crossChecked:crossChecked.length,combined:combined.length,uncovered:uncovered.length,dataUnavailable:dataUnavailable.length,unresolved:unresolved.length,ratio,crossRatio,pass}));
