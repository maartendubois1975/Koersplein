name: "ARCHIVED — Hindsight Amsterdam Brussel"

# Archived by Koersplein regie: no automatic triggers, no write permissions.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived workflow — intentionally inactive unless manually dispatched."
